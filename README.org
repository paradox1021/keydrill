#+TITLE: keydrill

* keydrill

Drill Emacs key bindings in short sessions until they stick.  Each
session introduces a few new moves, then recalls ones still being
drilled.  At session start, keydrill looks up each command in the
buffer you launched from and drills *your* binding when it differs
from the vanilla default.

Requires GNU Emacs 27.1 or later.  Licensed under GPL-3.0-or-later.

** Status

v0.1.0.  Not on MELPA yet (submission planned).  Home:
https://github.com/paradox1021/keydrill

** Install

On Emacs 29+, straight from this repository:

#+begin_src emacs-lisp
(package-vc-install "https://github.com/paradox1021/keydrill")
#+end_src

Or from a checkout, add it to ~load-path~ and load the package:

#+begin_src emacs-lisp
(add-to-list 'load-path "/path/to/keydrill")
(require 'keydrill)
#+end_src

Or with ~use-package~:

#+begin_src emacs-lisp
(use-package keydrill
  :load-path "/path/to/keydrill"
  :commands (keydrill keydrill-observe-mode keydrill-report
                      keydrill-purge-data))
#+end_src

** Usage

- ~M-x keydrill~ — start a session on the curated Emacs deck.  ~q~
  leaves the session.  A second ~C-g~ within one second also quits
  (~C-g~ itself is a drill answer when that is the card).
- ~M-x keydrill-benchmark~ — one cold recall pass over the whole
  deck, live-resolved: no answers shown, misses not requeued, the
  progress store untouched.  A measurement, not a lesson.  Run it
  once *before* you start drilling (that baseline is unrepeatable)
  and weekly after; each answer lands in the journal.
- ~M-x keydrill-purge-data~ — delete the local progress file after
  yes-or-no confirmation.
- ~M-x keydrill-journal-purge~ — delete the answer journal the same
  way.
- ~M-x keydrill-observe-mode~ — opt-in usage observer.  *Off by
  default.*  When on, it counts how commands are invoked (key, M-x,
  menu/mouse) into the same local file.  It does not record keystroke
  content, buffer text, or file names.
- ~M-x keydrill-report~ — unused-binding gaps from the observer
  (commands you run via M-x or the menu that already have a key).
  Jump from a row into a drill of those moves.

** Privacy

No account, no telemetry, no network.  Progress is a readable
~keydrill-data.eld~ in ~user-emacs-directory~, written with ~prin1~.
~M-x keydrill-purge-data~ deletes it.

Raw answers (move id, hit/miss, latency, timestamp — never keystroke
content or buffer text) are appended to a second readable file,
~keydrill-journal.eld~, so you can reconstruct your own progress
curves.  Set ~keydrill-journal-file~ to nil to disable;
~M-x keydrill-journal-purge~ deletes it.

To confirm there is no network code in the package files:

#+begin_src sh
grep -rn "url-retrieve\|url\\.el\|request" *.el
#+end_src

** Prior art

[[https://github.com/federicotdn/key-quiz][key-quiz]] (federicotdn) and keywiz already drill Emacs keys from a
fixed list.  keydrill adds live-keymap resolution (drill the binding
you actually use) and an opt-in usage-gap observer.

Prompts were adapted from my web trainer.

** Terminal notes

Capture folds terminal and GUI spellings of the same binding:
~C-/~ ↔ ~C-_~, ~C-SPC~ ↔ ~C-@~, ~TAB~ ↔ ~C-i~, and Escape-prefixed
Meta chords (~ESC x~ ↔ ~M-x~, ~ESC <~ ↔ ~M-<~, ~ESC C-\~ ↔ ~C-M-\~).
Live lookup skips menu-bar, mouse, and GUI names such as ~<open>~
(the File-open event); ~find-file~ drills ~C-x C-f~, not that event.

The landmine matrix is human, not CI.  Walk it in GUI Emacs and in
~emacs -nw~ before you trust a terminal.  Check each row in both:

| Binding | Why it is a landmine | GUI | tty |
|---------+----------------------+-----+-----|
| ~C-g~   | Must score as an answer, not ~keyboard-quit~ | | |
| ~C-x C-c~ | Must not kill Emacs | | |
| ~C-/~   | Arrives as ~C-_~ in many terminals | | |
| ~C-SPC~ | Arrives as ~C-@~ in many terminals | | |
| ~M-<~   | Arrives as ~ESC <~ | | |
| ~C-M-\~ | Arrives as ~ESC C-\~ | | |

The landmine matrix was hand-walked in GUI Emacs and ~emacs -nw~ on
Windows GNU Emacs 30.2 (2026-08-13); the hand pass caught two real
bugs the 114 ERT tests had not (a false "your binding" on stock
aliases like ~M-<~ / ~C-<home>~, and a summary that reported an
untimed session as 0 ms).

** How this was built

The code in this package was written with an AI agent.  The design,
the curated deck, the prompts (adapted from my web trainer), the
manual landmine testing, and the bug reports above are human work.
Review it as skeptically as you would any code, which is good advice
either way.

keydrill is GPL-3 and complete as-is — no accounts, no upsell, no
network code, and that will not change.  My company, Astrolabe Apps,
builds commercial team-training tools; keydrill remains free and
independent of them.

** MELPA recipe
:PROPERTIES:
:CUSTOM_ID: melpa-recipe
:END:

#+begin_src emacs-lisp
(keydrill :fetcher github :repo "paradox1021/keydrill")
#+end_src

The same form lives in the ~melpa-recipe~ file at the repo root.
MELPA generates ~keydrill-pkg.el~; this tree does not ship it.
Re-check the name ~keydrill~ against MELPA and GNU ELPA on
submission day.

** Development

#+begin_src sh
make compile lint test
#+end_src

~make compile~ byte-compiles with warnings as errors.  ~make lint~
runs checkdoc and package-lint.  ~make test~ runs the full ERT suite.

** License

GPL-3.0-or-later.  See ~LICENSE~.
